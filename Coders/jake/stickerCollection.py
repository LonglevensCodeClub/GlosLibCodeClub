class StickerBook():
    def __init__(self, name, collectionSize, packetSize):
        self.name = name
        self.collectionSize = collectionSize
        self.packetSize

class StickerCollection():
    def __init__(self, book):
        self.book = book
        self.stickers = dict()
        self.packetsBought = 0
        
    def buyPacket(self):
        self.packetsBought += 1
        
        
      
    while not (len(stickers) == booksise):
        stickersbought += 1
        
        for x in range(self.books.packetsize)
        s = randint(1, booksise)
        if s in stickers:
            stickers[s] += 1
        else:
            stickers[s] = 1  